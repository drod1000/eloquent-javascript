/*
Elements are stored as properties within arrays
Numbers, strings, and Booleans are immutable while objects can be modified by changing its properties
for(var event in map) allows us to loop through maps
Global scope object is stored in window
*/
